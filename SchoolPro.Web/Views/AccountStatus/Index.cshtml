@model IEnumerable<SchoolWiz.Web.Models.AccountStatus.AccountStatusIndexViewModel>

@{ ViewBag.Title = "Account Status List";}

<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a asp-controller="AccountStatus" asp-action="Index">Home</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Account Status List</li>
                    </ol>
                </nav><br /><br />
                <div class="row">
                    <div class="col-md-12 table-responsive-md">
                        <a class="btn btn-sm btn-outline-success float-md-right" asp-controller="AccountStatus" asp-action="Create"> New Account Status</a>
                        <table id="countryTable" class="table table-striped">
                            <thead>
                                <tr class="d-flex">
                                    <th class="col-4">Name</th>
                                    <th class="col-4">Inactive</th>
                                    <th class="text-warning col-4">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var accountStatus in Model)
                                {
                                    <tr class="d-flex">
                                        <td class="col-4" style="padding-right: 30px;">
                                            <a asp-controller="AccountStatus" asp-action="Edit" asp-route-id="@accountStatus.Id">
                                                @accountStatus.Name
                                            </a>
                                        </td>
                                        <td class="col-4" style="padding-right: 30px;">
                                            @accountStatus.InActive
                                        </td>
                                        <td class="col-4" style="padding-right: 30px;">
                                            <a class="text-primary" asp-action="Edit" asp-route-id="@accountStatus.Id">
                                                <i class="fas fa-edit"></i> Edit
                                            </a>
                                            <a class="text-danger" asp-action="Delete" asp-route-id="@accountStatus.Id">
                                                <i class="far fa-trash-alt"></i> Delete
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>